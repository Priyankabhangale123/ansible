---
- name: copy file from one directory to another 
  hosts: localhost
  tasks:
  - name: copy file from /root/ansible.txt to /home/ec2-user/aws/
    ansible.builtin.copy:
      scr: /root/ansible.txt
      dest: /home/ec2-user/aws/ansible.txt
